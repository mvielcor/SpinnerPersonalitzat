apply plugin: 'com.android.application'

android {
    compileSdkVersion 21
    buildToolsVersion "21.1.2"

    defaultConfig {
        applicationId "pmdm.mviel.navigationdrawler"
        minSdkVersion 21
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    // Biblioteca de Suport del Material Design
    compile 'com.android.support:design:22.2.0'
    // Bibliotea del Circle Image, que ens permet fer la imatge del perfil de forma redona.
    // extret de https://github.com/hdodenhof/CircleImageView
    compile 'de.hdodenhof:circleimageview:1.3.0'
    // Biblioteca del RecyclerView
    compile 'com.android.support:recyclerview-v7:21.0.0'

}
